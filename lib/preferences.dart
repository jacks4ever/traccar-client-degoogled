# Stage all changes
git add .

# Commit the changes with a message
git commit -m "Update Preferences.dart and fix build issues"

# Push the changes to the remote repository
git push origin main  # Replace 'main' with your branch name if different
